services:
  system:
    image: zhangjicmu/ubuntu24_ros:cmu_vla_challenge_simulation
    container_name: iros2026_system
    privileged: true
    network_mode: host
    environment:
      - DISPLAY
      - QT_X11_NO_MITSHM=1
      - XAUTHORITY=/tmp/.docker.xauth
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - /etc/localtime:/etc/localtime:ro
      - /dev/input:/dev/input
      - /dev/bus/usb:/dev/bus/usb
    stdin_open: true
    tty: true
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  ai_module:
    build:
      context: ../ai_module
      dockerfile: docker/Dockerfile
    container_name: iros2026_ai_module
    network_mode: host
    stdin_open: true
    tty: true
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  # Tmah 팀이 사용하는 docker-compose 파일 (0706 생성)
  tmah_module:
    build:
      context: ../ai_module
      dockerfile: docker/Dockerfile.tmah
    container_name: iros2026_tmah_module
    network_mode: host
    stdin_open: true
    tty: true
    volumes:
      - ../ai_module/debug:/home/docker/ai_module/debug
      - ../ai_module/src:/home/docker/ai_module/src
    deploy:
        resources:
          reservations:
            devices:
              - driver: nvidia
                count: all
                capabilities: [gpu]

  # sysnav MVP (YOLO-World + SAM2 + Gemini) - tmah_vlm과 별개 의존성 스택이라 독립 컨테이너로 분리 (0720 생성)
  sysnav_module:
    build:
      context: ../ai_module
      dockerfile: docker/Dockerfile.sysnav
    container_name: iros2026_sysnav_module
    network_mode: host
    stdin_open: true
    tty: true
    env_file:
      - ../ai_module/.env
    volumes:
      - ../ai_module/src/sysnav_ros2_mvp:/home/docker/ai_module/src/sysnav_ros2_mvp
      - ../ai_module/debug:/home/docker/ai_module/debug
    deploy:
        resources:
          reservations:
            devices:
              - driver: nvidia
                count: all
                capabilities: [gpu]